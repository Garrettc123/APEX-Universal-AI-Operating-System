<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext HTTP only to local/development backends so the app can talk
  to a FastAPI server running on the host machine (10.0.2.2 from the emulator)
  or a LAN device. Production HTTPS backends are unaffected.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
</network-security-config>
