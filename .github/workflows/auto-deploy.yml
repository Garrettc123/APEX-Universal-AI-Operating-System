# DISABLED: This workflow is superseded by ci-cd-pipeline.yml
# The docker-build job in ci-cd-pipeline.yml handles all Docker build & push.
# This file is kept for reference only and will NOT trigger any runs.
# on: {} disables all triggers.

name: Auto Deploy Pipeline (DISABLED - see ci-cd-pipeline.yml)

on: {}  # No triggers - workflow disabled

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false  # Never runs
    steps:
      - name: This job never runs
        run: echo "Use ci-cd-pipeline.yml instead"
