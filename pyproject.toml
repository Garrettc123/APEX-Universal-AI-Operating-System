[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apex-ai-os"
version = "1.0.0"
description = "APEX Universal AI Operating System"
requires-python = ">=3.10"

[project.scripts]
app = "app:app"
